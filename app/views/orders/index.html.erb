<%= render :partial => 'shared/index',
:locals => {:title => "A continuación se listan las solicitudes de orden de compra por parte del usuario: #{@user_profile.fullname}",
            :flag => true }%>
