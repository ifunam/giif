<div class="<%= set_row_class(record_counter) %>">
  <span class="id"><%= record.id %></span>
  <span class="description">
    <b>Productos:</b><br/>
    <%= numerate_list(record.products, :description)%>
    <b>Proveedores:</b>
    <%= numerate_list(record.providers, :name)%>
    <span style="margin-left: 50px; font-weight: bold;">
      Total: <%= record.total_price %>, Divisa: <%= record.currency_order.currency.name%>
    </span>
    <span id="icon">
      <%= link_to(image_tag("icon_details.png", :title => 'Ver más detalles', :size => '16x16'), :action => 'show', :id => record.id)%>
    </span>
  </span>
  <span class="date"><%= record.date.to_s :short %></span>
  <span class="date_update"><%= record.updated_at.to_s :date_only %></span>
  <span class="status" id="<%= record.dom_id + '_status' %>">
    <!-- <%= status_image_tag(record)%> -->
    <%= record.current_status %>
  </span>
  <span class="actions"><%= render :partial => 'actions', :locals => {:record => record}%></span>
  <!-- <span class="actions" id="<%= record.dom_id + '_actions' %>"> -->
    
    <%= image_tag('progress_bar3.gif', :id => 'order_status_loader_indicator', :style => 'display: none') %>
  </span>
</div>
