This lines will change soon!
<% status = activate_links(controller_name, record.current_status.downcase.split(/\s+/).join('_'))%>
  <%= link_to_status(status[0], 'Capturar datos', 'icon_edit.png', {:action => 'new', :id => record.id})%>
  <%= link_to_status(status[1], 'Editar', 'icon_edit.png', {:action => 'edit', :id => record.id})%>
  <%= link_to_status(status[2], 'Ver HTML', 'icon_details.png', {:action => 'show_preview', :id => record.id})%>
  <%= link_to_status(status[3], 'Ver PDF', 'icon_pdf.png', {:action => 'show_pdf', :id => record.id})%>
  <%= link_to_status(status[3], 'Confirmar adquisición', 'icon_pdf.png', {:action => 'confirm', :id => record.id})%>
