<% status = activate_links(backend_name+"_"+record.current_status.downcase.split(/\s+/).join('_'))%>
<span class=<%= "opacity_"+status[0].to_s%>>
  <%= link_to_status(status[0], 'Capturar datos', 'icon_edit.jpg', {:action => 'new', :id => record.id})%>
</span>
<span class=<%= "opacity_"+status[1].to_s%>>
  <%= link_to_status(status[1], 'Editar', 'icon_edit.jpg', {:action => 'edit', :id => record.id})%>
</span>
<span class=<%= "opacity_"+status[2].to_s%>>
  <%= link_to_status(status[2], 'Ver HTML', 'icon_details.jpg', {:action => 'show_preview', :id => record.id})%>
</span>
<span class=<%= "opacity_"+status[3].to_s%>>
  <%= link_to_status(status[3], 'Ver PDF', 'icon_pdf.jpg', {:action => 'show_pdf', :id => record.id})%>
</span>
<span class=<%= "opacity_"+status[4].to_s%>>
  <%= link_to_status(status[3], 'Confirmar adquisición', 'icon_pdf.jpg', {:action => 'confirm', :id => record.id})%>
</span>
