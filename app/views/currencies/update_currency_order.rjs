if @record.id == 6
   @record.name = nil
   @record.url = nil
   page.replace_html :currency_order, :partial => 'new'
else
   page.replace_html :currency_order, :partial => 'update_currency_order', :object => @order
end

