<%- content_for :head do -%>
  <script type="text/javascript" charset="utf-8">
   //<![CDATA[
     providerForm = new Subform('<%= escape_javascript(render(:partial => "provider", :object => OrderProvider.new, :locals => {:index => "INDEX"})) %>',<%= @order.providers.length %>,'providers');
     productForm = new Subform('<%= escape_javascript(render(:partial => "product", :object => OrderProduct.new, :locals => {:index => "INDEX"})) %>',<%= @order.products.length %>,'products');
   //]]>
  </script>
<%- end -%>

<%= error_messages_for 'order' %>
<% form_for(@order, :url => { :controller => :estimates, :action => :update }) do |f| -%>
<div id="data">
   <%= render :partial => 'orders/user_data', :object => @user_profile%>
   <%= render :partial => 'form', :locals => { :f => f } %><br/>

  <div style="padding-right: 150px; text-align: right;">
   	<%= f.submit "Guardar cambios" %>
   	<%= link_to 'Cancelar', estimates_path, :confirm => 'Al cancelar la orden de compra no será almacenada' %><br/><br/>
  </div>
  </p>
</div>
<% end %>
